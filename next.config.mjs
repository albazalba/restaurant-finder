/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['flowbite.s3.amazonaws.com','images.unsplash.com','dynamic-media-cdn.tripadvisor.com', 'media-cdn.tripadvisor.com'],
      },
};

export default nextConfig;
